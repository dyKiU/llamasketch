services:
  app:
    build: .
    env_file: .env
    environment:
      - PENCIL_GIT_COMMIT=${PENCIL_GIT_COMMIT:-dev}
    ports:
      - "${PENCIL_PORT:-8100}:8000"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
